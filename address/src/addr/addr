create table addr(
	num number(3),
	name varchar2(20) not null,
	phone varchar2(20) not null,
	phone varchar2(50) not null,
	email varchar2(50) not null,
	primary key(num)
);

create sequence addr_num_seq
start with 1
increment by 1;

select * from addr;